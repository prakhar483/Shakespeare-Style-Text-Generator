# Shakespeare-Style-Text-Generator
Generate Shakespeare-style text using a character-level Recurrent Neural Network (RNN). The model learns sequential patterns from Shakespeare’s writings to produce coherent, stylistically similar passages. Includes data preprocessing, model training, and text generation examples.
